<h1>Listing status_consulta</h1>

<table>
  <tr>
    <th>Id</th>
    <th>Nome</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @status_consulta.each do |status_consultum| %>
  <tr>
    <td><%= status_consultum.id %></td>
    <td><%= status_consultum.nome %></td>
    <td><%= link_to 'Show', status_consultum %></td>
    <td><%= link_to 'Edit', edit_status_consultum_path(status_consultum) %></td>
    <td><%= link_to 'Destroy', status_consultum, :method => :delete, :data => { :confirm => 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Status consultum', new_status_consultum_path %>
