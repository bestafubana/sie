<%= form_for(@status_consultum) do |f| %>
  <% if @status_consultum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@status_consultum.errors.count, "error") %> prohibited this status_consultum from being saved:</h2>

      <ul>
      <% @status_consultum.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :id %><br />
    <%= f.text_field :id %>
  </div>
  <div class="field">
    <%= f.label :nome %><br />
    <%= f.text_field :nome %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
